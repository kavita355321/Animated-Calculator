{
  "name": "precision-calculator",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Accessible calculator with a tested expression parser and no eval",
  "scripts": {
    "test": "node --test",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}

